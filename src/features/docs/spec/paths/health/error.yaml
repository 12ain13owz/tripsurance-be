get:
  tags:
    - Health
  summary: Health Error check
  operationId: healthErrorCheck
  responses:
    '400':
      description: Always throws — demonstrates the error envelope shape.
      content:
        application/json:
          schema:
            $ref: '../../components/models/error.schema.yaml'
          example:
            message: Test error function
            timestamp: '2025-05-29T10:00:00Z'
