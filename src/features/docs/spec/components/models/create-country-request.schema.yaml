type: object
required:
  - isoCode
properties:
  isoCode:
    type: string
    description: >-
      ISO 3166-1 alpha-2 code. Trimmed and uppercased before validation, so
      lowercase input is accepted.
    example: jp
